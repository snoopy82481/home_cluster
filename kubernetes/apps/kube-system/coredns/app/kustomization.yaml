---
apiVersion: kustomize.config.k8s.io/v1
kind: Kustomization
resources:
  - ./helm-release.yaml
configMapGenerator:
  - name: coredns-values
    files:
      - values.yaml=./helm/values.yaml
configurations:
  - ./helm/kustomizeconfig.yaml
