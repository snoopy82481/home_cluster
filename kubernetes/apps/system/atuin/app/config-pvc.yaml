---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: atuin-config-v1
  labels:
    app.kubernetes.io/name: &name atuin
    app.kubernetes.io/instance: *name
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: ceph-block
