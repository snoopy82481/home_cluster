---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./secret.sops.yaml
  - ./helm-release.yaml
namespace: media
configMapGenerator:
  - name: recyclarr-sonarr-configmap
    files:
      - sonarr-config.yaml
  - name: recyclarr-sonarr-anime-configmap
    files:
      - sonarr-anime-config.yaml
  - name: recyclarr-sonarr-uhd-configmap
    files:
      - sonarr-uhd-config.yaml
  - name: recyclarr-radarr-configmap
    files:
      - radarr-config.yaml
  - name: recyclarr-radarr-anime-configmap
    files:
      - radarr-anime-config.yaml
  - name: recyclarr-radarr-uhd-configmap
    files:
      - radarr-uhd-config.yaml
  - name: recyclarr-radarr-uhd-anime-configmap
    files:
      - radarr-uhd-anime-config.yaml
generatorOptions:
  disableNameSuffixHash: true
  annotations:
    kustomize.toolkit.fluxcd.io/substitute: disabled
