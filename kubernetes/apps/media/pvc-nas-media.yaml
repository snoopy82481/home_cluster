---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nas-data-media
  namespace: media
spec:
  resources:
    requests:
      storage: 1Mi
  storageClassName: ""
  volumeName: nas-data-media
  accessModes:
    - ReadWriteMany
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nas-backup-media
  namespace: media
spec:
  resources:
    requests:
      storage: 1Mi
  storageClassName: ""
  volumeName: nas-backup-media
  accessModes:
    - ReadWriteMany
