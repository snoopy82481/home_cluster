---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ${APP}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: ${STORAGE_CAPACITY:=5Gi}
  storageClassName: ${STORAGECLASS:=ceph-block}
