---
apiVersion: v1
kind: Pod
metadata:
  name: disk-clean-kworker3
  namespace: default
spec:
  restartPolicy: Never
  volumes:
    - name: rook-data-dir
      hostPath:
        path: /var/lib/rook
  nodeName: kworker3
  containers:
    - name: disk-clean
      image: busybox
      securityContext:
        privileged: true
      volumeMounts:
        - name: rook-data-dir
          mountPath: /node/rook-data
      command: ["/bin/sh", "-c", "rm -rf /node/rook-data/*"]
      resources:
        requests:
          cpu: 10m
          memory: 250Mi
        limits:
          memory: 500Mi
          cpu: 20m
