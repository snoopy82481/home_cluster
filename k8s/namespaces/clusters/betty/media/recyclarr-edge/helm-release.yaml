---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: &app recyclarr-edge
  namespace: media
spec:
  interval: 5m
  chart:
    spec:
      chart: app-template
      version: 1.0.1
      sourceRef:
        kind: HelmRepository
        name: bjw-s-charts
        namespace: flux-system
      interval: 5m
  test:
    enable: false
  install:
    remediation:
      retries: 5
  upgrade:
    remediation:
      retries: 5
      remediateLastFailure: true
    cleanupOnFail: true
  rollback:
    timeout: 10m
    recreate: true
    cleanupOnFail: true
  values:
    image:
      repsitory: recyclarr/recyclarr
      tag: edge@sha256:27925683a7bf9ee0fac3fd6f2fed71ad81aa4a06dd272d8c33519841dc5d6970

    env:
      TZ: ${TIMEZONE}
      CRON_SCHEDULE: "@daily"

    persistence:
      config:
        enabled: true
        type: configMap
        name: recyclarr-edge
        subPath:
          - path: /config/recyclarr.yaml
      secret:
        enabled: true
        type: secret
        name: secret
        subPath:
          - path: /config/secret.yaml
