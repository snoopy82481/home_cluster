---
apiVersion: v1
kind: Service
metadata:
  name: &app octoprint-e5s1
  namespace: networking
  labels:
    app.kubernetes.io/name: *app
    app.kubernetes.io/instance: *app
spec:
  type: ExternalName
  externalName: octoprint-e5s1.
  ports:
    - name: http
      port: &port 82
      targetPort: *port
      protocol: TCP
